# ----------
# src/test/trace/trace_schedule
#
# Each test here is run through psql with PQTRACE enabled, and the
# resulting protocol trace is compared against expected/<test>.trace.
# ----------

test: protocol_compression_switch protocol_compression_lz4 protocol_compression_zstd
